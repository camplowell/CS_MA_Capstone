using UnityEngine;

public static class ChangeTransitionVisibility {
    public static void SetVisibility(GameObject obj, bool visibleInside, bool visibleOutside) {
        foreach (var renderer in obj.GetComponents<Renderer>()) {

        }
    }
}